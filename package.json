{
  "name": "climbing-notes-pwa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf docs",
    "build": "npm run clean && tsc && npm run copy",
    "copy": "node scripts/build-config.js && cp manifest.webmanifest docs && cp -R assets docs/",
    "preview": "npm run build && python3 -m http.server 8000 -d docs",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "convex": "^1.31.2"
  }
}
