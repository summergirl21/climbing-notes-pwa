{
  "name": "climbing-notes-pwa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && npm run copy",
    "copy": "mkdir -p dist && cp index.html manifest.webmanifest dist && cp -R assets dist/",
    "preview": "npm run build && python3 -m http.server 8000 -d dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
